[project]
name = "libtriton"
version = "0.1.0"

[build-system]
requires = ["cmake", "ninja", "scikit-build-core"]
build-backend = "scikit_build_core.build"

[tool.scikit-build]
wheel.install-dir = "libtriton/_C"
wheel.packages = ["python/libtriton"]
